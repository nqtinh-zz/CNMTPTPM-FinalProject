const initialState = {
    publickey: '',
    amount: 0,
    privatekey: '',
};

export default function (state = initialState, action) {
    switch (action.type) {
        default: return state;
    }
};